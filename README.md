## 🚀 Quick Start

```bash
# 1. Clone
git clone https://github.com/<you>/cracklab.git
cd cracklab

# 2. (Optional) create a virtual env
python -m venv .venv && source .venv/bin/activate

# 3. Install backend deps
pip install -r requirements.txt   # Flask, Flask-SocketIO, etc.

# 4. Run
python app.py          # http://127.0.0.1:5000
